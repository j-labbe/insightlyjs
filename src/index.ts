import InsightlyJS from './InsightlyJS';
import {
    Activity,
    ActivitySet,
    Comment,
    Contact,
    CustomField,
    Email,
    InsightlyEvent,
    InsightlyDate,
    FileAttachment,
    Link,
    Note,
    Tag,
    Task,
} from './types';
export default InsightlyJS;
export { Activity, ActivitySet, Comment, Contact, CustomField, Email, InsightlyEvent, FileAttachment, InsightlyDate, Link, Note, Tag, Task };
